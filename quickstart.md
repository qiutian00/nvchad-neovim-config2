# Neovim 新手使用文档（NvChad v2.5）

> 本机配置基于 **NvChad v2.5**，主题为 **onedark**，使用 **lazy.nvim** 管理插件。
> 本文面向完全没接触过 Vim/Neovim 的新手，从零开始讲解。

---

## 目录

1. [这是什么](#1-这是什么)
2. [前置环境要求](#2-前置环境要求)
3. [第一次启动](#3-第一次启动)
4. [核心概念：模式（Mode）](#4-核心概念模式mode)
5. [新手生存必备命令](#5-新手生存必备命令)
6. [Leader 键与快捷键总览](#6-leader-键与快捷键总览)
7. [各功能模块详解](#7-各功能模块详解)
8. [常见任务操作指南](#8-常见任务操作指南)
9. [故障排查](#9-故障排查)
10. [如何自定义配置](#10-如何自定义配置)

---

## 1. 这是什么

Neovim 是一个终端/命令行界面的文本编辑器，是经典 Vim 的现代化版本。
你现在的这套配置叫 **NvChad**，它是一套"开箱即用"的 Neovim 发行版，把：

- 好看的界面（主题、状态栏、顶部标签栏）
- 文件树、全局搜索
- 代码补全、语法高亮、格式化
- Git 集成、终端

等功能预先打包配置好了，你只需要记几个快捷键就能高效使用。

### 本机配置概况

| 项目 | 值 |
|------|-----|
| 基础框架 | NvChad v2.5 |
| 主题 | onedark |
| 插件管理器 | lazy.nvim |
| Leader 键（前缀键） | 空格 `Space` |
| 已启用 LSP 语言服务器 | html、css、lua |
| 已启用格式化 | Lua 用 stylua |
| Neovim 版本 | 0.12.5 |

---

## 2. 前置环境要求

- **Neovim ≥ 0.10**（本机 0.12.5，已满足）
- **Git**（用于插件下载，本机已装）
- **ripgrep**（用于全局搜索 `Telescope live_grep`，本机已装，命令为 `rg`）
- **Nerd Font 字体**（重要！）界面上的图标（文件夹、箭头、Git 符号等）依赖 Nerd Font 字体。
  推荐 `CaskaydiaCove Nerd Font` 或 `JetBrainsMono Nerd Font`，并在 Windows Terminal / 你使用的终端里把字体设成它，否则会看到乱码方块。

> 检查方法：打开 Neovim，如果界面图标显示为 `□` 或 `?`，说明终端没设置 Nerd Font。

---

## 3. 第一次启动

在终端里输入：

```powershell
nvim
```

首次启动会自动下载插件（需要联网），等待片刻即可。出现仪表盘/空白界面说明加载成功。

- 想打开某个文件：`nvim 文件名`
- 想打开一个目录：`nvim .`（会在当前目录启动）

---

## 4. 核心概念：模式（Mode）

Vim 最大的特点就是"模式切换"，新手最先要理解这一点。左下角的状态栏会显示当前模式。

| 模式 | 进入方式 | 用途 |
|------|----------|------|
| **普通模式 (NORMAL)** | 启动默认 / 按 `Esc` | 移动光标、执行命令、删改 |
| **插入模式 (INSERT)** | 按 `i` | 输入文字 |
| **可视模式 (VISUAL)** | 按 `v` | 选中文本 |
| **命令行模式 (CMD)** | 按 `:` | 输入命令（保存、退出等） |
| **终端模式 (TERMINAL)** | 通过快捷键打开内置终端 | 运行 shell 命令 |

**新手最容易困惑的点**：Vim 默认是"普通模式"，不能直接打字。想打字先按 `i` 进入插入模式；打完按 `Esc`（或本配置里的 `jk`）回到普通模式。

---

## 5. 新手生存必备命令

学会下面这些，你就能"用起来不慌"。

### 5.1 模式切换

| 快捷键 | 说明 |
|--------|------|
| `i` | 进入插入模式（光标前） |
| `a` | 进入插入模式（光标后） |
| `o` | 在下方新起一行并进入插入模式 |
| `Esc` | 回到普通模式 |
| `jk` | 插入模式下快速回到普通模式（本配置自定义） |
| `v` | 进入可视模式（选中） |
| `V` | 整行选中 |
| `:` | 进入命令行模式 |
| `;` | 等价于 `:`（本配置自定义，少按一个 Shift） |

### 5.2 移动光标（普通模式）

| 按键 | 说明 |
|------|------|
| `h` `j` `k` `l` | 左 / 下 / 上 / 右 |
| `w` / `b` | 跳到下一个 / 上一个单词开头 |
| `0` / `$` | 行首 / 行尾 |
| `gg` / `G` | 文件开头 / 文件末尾 |
| `Ctrl+d` / `Ctrl+u` | 向下 / 向上翻半页 |
| `数字 + j/k` | 例如 `5j` 向下移动 5 行 |

### 5.3 编辑操作（普通模式）

| 按键 | 说明 |
|------|------|
| `x` | 删除光标下字符 |
| `dd` | 删除整行 |
| `yy` | 复制整行 |
| `p` | 粘贴 |
| `u` | 撤销 |
| `Ctrl+r` | 重做 |
| `cc` | 删除整行并进入插入模式 |
| `ciw` | 删除当前单词并进入插入模式 |

### 5.4 保存 / 退出（命令行模式，先按 `:` 或 `;`）

| 命令 | 说明 |
|------|------|
| `:w` | 保存 |
| `:q` | 退出 |
| `:qall` | 全部退出 |
| `:wq` | 保存并退出 |
| `:q!` | 不保存强制退出 |
| `:wqa` | 保存并退出所有文件 |
| `Ctrl+s` | 快速保存（普通模式下，本配置自带） |

---

## 6. Leader 键与快捷键总览

**Leader 键 = 空格键**。很多快捷键是"先按空格，再按一个字母"的组合。
按空格后稍作停留，会弹出 **which-key** 提示面板，显示所有可用的后续按键——新手不需要死记，看提示即可。

> 例如 `<leader>ff` 表示：按一下空格，松开，再按 `f`，再按 `f`， 搜索文件目录。

### 6.1 通用

| 快捷键 | 说明 |
|--------|------|
| `<leader>ch` | 打开内置快捷键速查表（NvCheatsheet，强烈推荐先看这个） |
| `<leader>n` | 切换行号显示 |
| `<leader>rn` | 切换相对行号 |
| `<leader>th` | 切换主题 |
| `<leader>fm` | 格式化当前文件（普通/可视模式都可用） |
| `<leader>wK` | 列出所有快捷键 |
| `<leader>wk` | 搜索某个快捷键 |
| `<Esc>` | 清除搜索高亮 |

### 6.2 缓冲区 / 标签页（Buffer）

顶部标签栏（tabufline）每个标签对应一个打开的文件。

| 快捷键 | 说明 |
|--------|------|
| `<leader>b` | 新建空白缓冲区 |
| `<Tab>` | 切换到下一个缓冲区 |
| `<Shift+Tab>` | 切换到上一个缓冲区 |
| `<leader>x` | 关闭当前缓冲区 |

### 6.3 文件树（nvim-tree）

| 快捷键 | 说明 |
|--------|------|
| `Ctrl+n` | 打开/关闭文件树 |
| `<leader>e` | 跳转到文件树窗口 |

在文件树窗口内常用按键：`a` 新建文件、`r` 重命名、`d` 删除、`Enter` 打开、`H` 显示/隐藏隐藏文件。

### 6.4 文件查找与搜索（Telescope）

Telescope 是全局搜索神器，弹窗内可用 `Ctrl+j`/`Ctrl+k` 上下移动、`Enter` 确认、`Esc` 关闭。

| 快捷键 | 说明 |
|--------|------|
| `<leader>ff` | 按文件名查找文件 |
| `<leader>fw` | 全文内容搜索（live grep） |
| `<leader>fz` | 在当前文件内模糊搜索 |
| `<leader>fa` | 查找所有文件（含隐藏、忽略的文件） |
| `<leader>fb` | 在所有已打开缓冲区中查找 |
| `<leader>fo` | 最近打开过的文件 |
| `<leader>fh` | 搜索帮助文档 |
| `<leader>ma` | 查找标记（marks） |

### 6.5 Git 相关

| 快捷键 | 说明 |
|--------|------|
| `<leader>cm` | 查看 Git 提交历史 |
| `<leader>gt` | 查看 Git 状态（改动的文件） |
| `<leader>/` | 注释/取消注释当前行或选中块 |

### 6.6 LSP（代码智能）

LSP 提供跳转定义、重命名、错误诊断等能力（当前启用 html、css、lua）。

| 快捷键 | 说明 |
|--------|------|
| `gd` | 跳转到定义 |
| `gD` | 跳转到声明 |
| `<leader>D` | 跳转到类型定义 |
| `<leader>ra` | 重命名符号（变量、函数等） |
| `<leader>ds` | 把诊断信息列到位置列表 |
| `<leader>wa` | 添加工作区文件夹 |
| `<leader>wr` | 移除工作区文件夹 |
| `<leader>wl` | 列出工作区文件夹 |

### 6.7 终端

| 快捷键 | 说明 |
|--------|------|
| `<leader>h` | 在下方水平打开新终端 |
| `<leader>v` | 在右侧垂直打开新终端 |
| `<Alt+h>` | 切换（开/关）水平终端 |
| `<Alt+v>` | 切换（开/关）垂直终端 |
| `<Alt+i>` | 切换（开/关）浮动终端 |
| `<leader>pt` | 在所有隐藏终端中挑选 |
| `Ctrl+x` | 终端模式下退出到普通模式（这样才可用 `jk` 等） |

### 6.8 窗口管理

| 快捷键 | 说明 |
|--------|------|
| `Ctrl+h` `Ctrl+l` `Ctrl+j` `Ctrl+k` | 在窗口之间切换（左/右/下/上） |

### 6.9 本配置自定义的快捷键

| 快捷键 | 说明 |
|--------|------|
| `;` | 等价于 `:`（进入命令行模式，不用按 Shift） |
| `jk` | 插入模式下快速退出到普通模式 |

---

## 7. 各功能模块详解

### 7.1 仪表盘 / 速查表

- 启动时的首页叫 **nvdash**（本配置默认关闭，如需开启见第 10 节）。
- 输入 `:Nvdash` 可随时打开仪表盘。
- 输入 `:NvCheatsheet` 或按 `<leader>ch` 打开快捷键速查表。

### 7.2 代码补全（nvim-cmp）

在插入模式下输入代码时会自动弹出补全菜单，来源包括：LSP、缓冲区、路径、代码片段。

| 操作 | 说明 |
|------|------|
| `Ctrl+n` / `Ctrl+p` | 下一个 / 上一个候选项 |
| `Enter` | 确认选择 |
| `Tab` / `Shift+Tab` | 在补全菜单与代码片段占位符之间跳转 |
| `Esc` | 关闭补全菜单 |

### 7.3 自动括号配对（nvim-autopairs）

输入 `(`、`[`、`{`、`"`、`'` 时会自动补全右半部分，输入时会自动跳过右括号。

### 7.4 语法高亮（nvim-treesitter）

自动为各语言提供精确的高亮、折叠、缩进。首次打开某种语言的文件会自动下载对应解析器。

### 7.5 格式化（conform.nvim）

按 `<leader>fm` 手动格式化。当前配置了对 Lua 的 `stylua` 格式化器。

### 7.6 Git 集成（gitsigns）

编辑 Git 仓库中的文件时，行号左侧会显示：

- 绿色 `+`：新增行
- 红色 `-`：删除行
- 蓝色 `~`：修改行

### 7.7 语言服务器安装（Mason）

想给新语言（Python、JavaScript 等）加补全/跳转，用 Mason 安装对应 LSP：

```vim
:Mason
```

在打开的界面里找到想要的服务器，按 `i` 安装。

---

## 8. 常见任务操作指南

### 任务 1：打开一个项目并开始工作

```powershell
cd 你的项目目录
nvim .
```

然后 `Ctrl+n` 打开文件树，或用 `<leader>ff` 快速找文件。

### 任务 2：在整个项目里搜索某个关键词

按 `<leader>fw`，输入关键词，实时显示匹配结果，回车跳转。

### 任务 3：格式化代码

按 `<leader>fm`。

### 任务 4：多文件同时编辑

用 `<leader>ff` 打开多个文件，然后用 `<Tab>` / `<Shift+Tab>` 在它们之间切换；`<leader>x` 关闭当前文件。

### 任务 5：一边看代码一边跑命令

按 `<leader>v`（或 `<Alt+i>`）打开终端，跑命令；按 `Ctrl+x` 退出终端回到代码窗口。

---

## 9. 故障排查

| 现象 | 原因 / 解决 |
|------|-------------|
| 界面图标显示乱码方块 | 终端没使用 Nerd Font 字体。在终端设置里把字体换成 Nerd Font。 |
| 首次启动卡住/报错 | 一般是网络问题下载插件失败。重试 `nvim`，或手动 `git clone` 相关插件仓库。 |
| 某个语言的补全/跳转不工作 | 该语言的 LSP 未安装。用 `:Mason` 安装对应服务器。 |
| 全文搜索（`<leader>fw`）报错 | 未安装 ripgrep。`scoop install ripgrep` 或从官网安装。 |
| 改了配置不生效 | 在 Neovim 里执行 `:so` 重新加载，或直接退出重进。 |
| 插件更新 | 输入 `:Lazy` 打开插件管理器，按 `U` 更新，`i` 安装，`x` 清理。 |
| 查看健康状态 | 输入 `:checkhealth` 检查各项环境依赖。 |

---

## 10. 如何自定义配置

所有自定义文件都在配置目录 `~/AppData/Local/nvim/` 下（本机路径 `C:\Users\admin\AppData\Local\nvim\`）。

| 文件 | 用途 |
|------|------|
| `lua/chadrc.lua` | 主题、界面选项（改主题、开仪表盘等） |
| `lua/mappings.lua` | 自定义快捷键 |
| `lua/options.lua` | 编辑器基础选项（行号、缩进等） |
| `lua/plugins/init.lua` | 添加/配置插件 |
| `lua/configs/lspconfig.lua` | 配置 LSP 语言服务器 |
| `lua/configs/conform.lua` | 配置格式化器 |

### 常见修改示例

**1. 更换主题**（编辑 `lua/chadrc.lua`）：

```lua
M.base46 = {
  theme = "onedark",   -- 改成其他主题名，如 "catppuccin"、"tokyonight" 等
}
```

可输入 `:Telescope themes` 或按 `<leader>th` 实时预览所有主题。

**2. 开机显示仪表盘**（编辑 `lua/chadrc.lua`，取消注释）：

```lua
M.nvdash = { load_on_startup = true }
```

**3. 增加语言服务器**（编辑 `lua/configs/lspconfig.lua`）：

```lua
local servers = { "html", "cssls", "pyright" }  -- 加上 pyright 支持 Python
vim.lsp.enable(servers)
```

改完后用 `:Mason` 安装对应服务器。

**4. 增加格式化器**（编辑 `lua/configs/conform.lua`）：

```lua
formatters_by_ft = {
  lua = { "stylua" },
  css = { "prettier" },
  html = { "prettier" },
},
```

---

## 附：核心快捷键速查卡（可打印）

```
模式： i 插入 / v 可视 / : 命令 / Esc 普通
移动： h j k l / w b / gg G / Ctrl+d u
保存退出： :w :q :wq :q!   Ctrl+s 保存
Leader=空格，按空格看提示
编辑： x dd yy p u Ctrl+r ciw

文件树    Ctrl+n         查找文件  <leader>ff
全文搜索  <leader>fw     找缓冲区  <leader>fb
最近文件  <leader>fo     注释      <leader>/
格式化    <leader>fm     主题      <leader>th
跳定义    gd            重命名    <leader>ra
切换标签  Tab/Shift+Tab 关标签    <leader>x
新建标签  <leader>b      终端      <leader>v/<leader>h/<Alt+i>
切换窗口  Ctrl+h/j/k/l   速查表    <leader>ch
```